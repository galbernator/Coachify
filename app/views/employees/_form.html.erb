<%= simple_form_for (@employee, html: {class: "form-horizontal"} do |f| %>
  <%= f.input :first_name %>
  <%= f.input :first_name %>
  <%= f.hidden :location, value: @location %>
  <%= f.submit %>
<% end %>
