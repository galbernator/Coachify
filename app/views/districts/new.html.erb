<div class="row top-div">
  <div class="col-xs-offset-1 col-xs-10 col-sm-offset-3 col-sm-6">
    <%= simple_form_for([@company, @district], html: {class: "form-horizontal"}) do |f| %>
      <%= f.input :name %>
      <%= f.submit "Add District", class: "form-control submit-button" %>
    <% end %>
  </div>
</div>
